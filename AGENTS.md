# Orbit 项目协作说明

## 产品边界

- 核心链路是自然语言输入、生成单文件网页、沙箱预览、多轮修改、持久化和版本恢复。
- 生成产物限定为完整 HTML、内联 CSS 和原生 JavaScript。
- 不为生成产物安装依赖，不运行用户生成的服务端代码。
- 界面中的功能状态必须真实，不展示虚构百分比或未实现能力。
- 第一阶段允许使用明确标注的本地生成器；接入模型后必须真实调用服务端接口。

## 技术约束

- 使用 Next.js、TypeScript、React 和 Tailwind CSS。
- 模型密钥只放在服务端环境变量中。
- 预览使用 `iframe srcDoc` 与 `sandbox="allow-scripts"`，不能加入 `allow-same-origin`。
- HTML 输出需要长度限制、结构校验和外部资源检查。
- 持久化使用 Supabase，并对用户数据启用行级权限。
- 每次成功生成创建不可变版本，更新当前版本失败时保留旧版本。

## 交互与视觉

- 工作台是首屏主界面，不增加营销页作为前置步骤。
- 重要操作应有加载、成功、空状态和错误状态。
- 保持键盘操作、清晰焦点样式、足够对比度和移动端适配。

## 验证命令

- 安装依赖：`npm ci`
- 本地开发：`npm run dev`
- 类型检查：`npm run typecheck`
- 生产构建：`npm run build`
- 静态检查：`npm run lint`

完成一个阶段前，至少运行生产构建，并在浏览器中实际操作该阶段的核心流程。

## 完成报告

每个阶段结束时说明完成的用户可见功能、运行过的检查和结果、尚需用户提供的外部配置、已知限制和下一阶段范围。
