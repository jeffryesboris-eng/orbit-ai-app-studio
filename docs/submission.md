# Orbit 笔试提交说明

## 提交链接

- 在线 Demo：https://orbit-ai-app-studio.vercel.app
- GitHub 源代码：https://github.com/jeffryesboris-eng/orbit-ai-app-studio

## 实现思路

Orbit 将一次生成限制为完整的单文件 HTML。用户需求由 Next.js 服务端接口发送给模型，模型统一返回 `title`、`summary` 和 `html`。服务端对结构、长度和危险能力进行校验，通过后才把结果交给浏览器。生成页面在 `sandbox="allow-scripts"` 的 iframe 中运行，无法读取主应用身份或服务端密钥。

Supabase Auth 为首次访问者创建匿名身份。项目、成功对话和每次生成的完整 HTML 分别保存在 `projects`、`messages` 和 `code_versions` 中。数据库函数在同一事务里保存一次成功生成，RLS 使用 `auth.uid()` 隔离用户数据。历史恢复只切换当前版本指针，不修改旧版本。

## 当前完成程度

- 已完成：真实模型生成、沙箱预览、生成应用交互、多轮修改。
- 已完成：匿名身份、项目和消息持久化、不可变版本、刷新恢复、历史恢复。
- 已完成：输入与输出限制、服务端超时、基础频率限制、无效模型结果校验、失败后保留旧预览和重试。
- 已完成：桌面与移动端适配、代码查看与复制、加载、错误和空状态。
- 已完成：Vercel 函数时限配置、环境检查脚本、部署和验收说明。
- 未包含：支付、多人协作、复杂第三方登录、任意依赖安装和服务端代码执行。

## 关键取舍

1. 只生成单文件 HTML、内联 CSS 和原生 JavaScript，以换取稳定、快速且容易隔离的演示体验。
2. 使用 iframe `srcDoc` 和最小 sandbox 权限执行生成代码，并在服务端阻止外部资源与网络 API。
3. 使用 Supabase 匿名身份降低首次体验门槛；数据与当前浏览器身份绑定。
4. 使用数据库函数保证项目、两条消息和代码版本一起写入，避免部分保存。
5. 当前频率限制保存在单个运行实例内，足够用于受控演示；公开规模化使用需要共享限流。

## 验证记录

- `npm run check:env`：通过。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm run build`：通过。
- 浏览器主流程：首次生成、预览交互、继续修改、刷新恢复、查看两个历史版本、恢复旧版本并再次刷新，全部通过。
- 线上生产环境：成功生成“汇率换算器”，刷新后项目、消息和预览均从 Supabase 恢复。
- 浏览器控制台：未发现错误或警告。

## 已知限制

- 匿名会话存于浏览器；清除站点数据或更换设备后无法找回原身份。
- Vercel 多实例之间不共享内存限流计数。
- 生成结果限定为单页应用，不能安装 npm 包或访问外部网络资源。
- 模型质量和响应时间受所选模型服务影响。

## 继续投入时的优先级

| 优先级 | 扩展方向 | 判断依据 |
| --- | --- | --- |
| P0 | 用 Redis 或数据库实现按用户/IP 的共享限流与每日额度 | 当前内存限流无法覆盖 Vercel 多实例；公开投放前应先控制模型成本与滥用风险。 |
| P0 | 增加 Cloudflare Turnstile、匿名数据过期清理和基础滥用监控 | 匿名体验门槛低，但也容易被批量创建账号；这是扩大访问量前的稳定性要求。 |
| P1 | 支持匿名账号升级为邮箱或 OAuth 账号，并保留已有项目 | 让用户能跨设备继续使用，同时不破坏当前无需注册的首次体验。 |
| P1 | 增加项目重命名、删除和版本差异比较 | 完善已有项目与版本模型，提升持续创作效率，技术风险较低。 |
| P2 | 提供模板库、作品分享和一键复制项目 | 有助于传播和新用户上手，但应在成本控制与账号体系稳定后投入。 |
| P2 | 在独立执行环境中支持多文件项目和受控依赖 | 能显著扩大应用复杂度，但会引入构建、隔离、资源配额和供应链安全成本。 |
